{
  "name": "mission-control-os",
  "version": "0.1.0",
  "private": true,
  "description": "Mission Control OS — an open-source agentic mission-control platform. Ideas become blueprints, blueprints become governed missions.",
  "license": "Apache-2.0",
  "scripts": {
    "dev:web": "pnpm --filter web dev",
    "build:web": "pnpm --filter web build",
    "lint:web": "pnpm --filter web lint"
  },
  "engines": {
    "node": ">=20"
  }
}
